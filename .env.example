# Optional for this synthetic-data demo; required for stable audit correlation.
# Use a secret value with at least 32 bytes and never commit the real key.
AUDIT_PSEUDONYMIZATION_KEY=replace-with-a-secret-value-of-at-least-32-bytes
